How to run:

Open visual studio .sln file.  Right click on solution and click properties.  Make sure configuration is multple startup projects with both the server and client projects selected.

How to test:

Open test explorer and run xunit tests.

Alternatively, create json models and pass in using swagger or postman.  The xunit tests don't test the api endpoints.